# api-server service settings:
#   Root Directory     = /   (must be repo root)
#   Config-as-code     = /railway.toml  (this file)

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
