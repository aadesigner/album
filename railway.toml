# api-server service settings:
#   Root Directory = /   (must be repo root)
#   Builder will use this Dockerfile (not Railpack/pnpm frozen install)

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
